import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juangamonoso.noticias',
  appName: 'Noticias',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
